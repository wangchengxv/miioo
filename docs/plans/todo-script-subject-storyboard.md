# Todo：剧本、主体与分镜

## 1. 剧本与分集

| 事项 | 层级 | 当前状态 | 依赖 | 事实源 | 验收口径 |
|---|---|---|---|---|---|
| 剧本 SSE 流式生成真实环境回归 | 页面 + 适配层 + 后端 | 待联调 | 文本模型、SSE、token 刷新 | 剧本映射模块 | 流式内容稳定输出且可保存 |
| 整稿工作区 `chat -> split-preview -> finalize` 回归 | 页面 + 后端 | 待联调 | 默认 chat 模型 | 剧本映射模块 | 可从整稿生成正式分集并回显 |
| 剧本文本模型可用性探测脚本 | 后端 + 文档 | 已完成 | 本地 DB、已配置 provider/key | `backend/scripts/test_script_models.py` | 可批量输出各 chat 模型在“整稿生成 + 拆分预览”链路下的可用/不可用结果与错误原因 |
| 目标集数透传回归 | 页面 + 适配层 + 后端 | 待验证 | `episode_count` | 剧本映射备注 | 固定集数和自定义集数都生效 |
| 剧本上传链路回归 | 页面 + 后端 | 待联调 | 上传接口 | 剧本映射模块 | 上传后能正确生成或更新分集内容 |
| 提取主体同步链路回归 | 页面 + 后端 | 待联调 | chat 模型、`subjects/extract` | 剧本映射备注 | 同步返回主体数组，页面刷新后角色/场景/道具回显正确 |

## 2. 主体

| 事项 | 层级 | 当前状态 | 依赖 | 事实源 | 验收口径 |
|---|---|---|---|---|---|
| 角色 / 场景 / 道具 CRUD 回归 | 页面 + 适配层 | 待联调 | `episode_id` 映射 | 主体映射模块 | 新增、编辑、删除后刷新一致 |
| 参考图上传 / 绑定回归 | 页面 + 后端 | 待联调 | 上传接口 | 主体映射备注 | 参考图可上传、绑定并刷新回显 |
| 单主体生成图任务制回归 | 页面 + 后端 | 待联调 | 图片模型、任务接口 | 主体映射备注 | 返回 `taskId`、中间状态和终态正确 |
| 批量生成任务制回归 | 页面 + 后端 | 待联调 | 图片模型、任务接口 | 主体映射备注 | 能显示成功 / 失败数量，不无限轮询 |
| 主图切换与候选图下载回归 | 页面 + 适配层 | 待验证 | 下载接口 | 主体映射备注 | 定稿后主图回显正确且下载可用 |
| 音色真实来源回归 | 页面 + 适配层 + 后端 | 待验证 | `GET /api/voices` | 主体映射备注 | 页面展示和弹窗选择都来自真实音色源 |

## 3. 分镜

| 事项 | 层级 | 当前状态 | 依赖 | 事实源 | 验收口径 |
|---|---|---|---|---|---|
| 分镜 CRUD 与排序回归 | 页面 + 适配层 | 待联调 | `storyboards` 接口 | 分镜映射模块 | 新增、插入、复制、删除、排序后刷新一致 |
| 智能分镜任务制回归 | 页面 + 后端 | 待联调 | tasks 接口、文本模型 | 分镜映射备注 | 立即返回 `taskId`，分镜按 chunk 逐步回显 |
| `409` 已有分镜路径验证 | 页面 + 后端 | 待验证 | 智能分镜生成接口 | 分镜专项备注 | 已存在分镜时不会重复追加脏数据 |
| `partial` 终态提示验证 | 页面 + 后端 | 待验证 | tasks 接口 | 分镜映射备注 | 部分完成时能停轮询并提示成功 / 失败数量 |
| 分镜图生成回归 | 页面 + 后端 | 待联调 | 图片模型、参考图能力 | 分镜映射模块 | 生图成功、回显稳定、参考图限制提示正确 |
| 分镜视频生成回归 | 页面 + 后端 | 待联调 | 视频模型能力、公网媒体 | 分镜映射模块 | 时长 / 分辨率 / 参考模式都能正确提交并回显 |
| 任务2 UI 承接细化 | 页面 + 适配层 | 进行中 | 分镜双层字段方案 | `SBX-04` | 人工稿、自动稿、脏标记和恢复逻辑清晰可用 |
| 下游入口统一消费新口径 | 页面 + 适配层 + 后端 | 进行中 | `manual > auto > imagePrompt/description` | `SBX-05` | 图片 / 视频 / 配音入口都统一遵循同一优先级 |
