# 前端进度：创作、资产与剪辑

## 1. 范围

本文件收口以下高信息量模块的前端进度：

- 创作页
- 资产库
- 剪辑成片
- 音色库 / 参考音频库弹窗承接

## 2. 当前状态

| 模块 | 当前状态 | 备注 |
|---|---|---|
| 创作页 | 已打通，待真实联调 | 图片/视频/配音三工作流、统一素材入口、详情弹窗、任务回流已接通；`2026-06-11` 起普通用户可共享复用管理员账号已启用的 `AI Ping / MiniMax-Speech-2.8-hd` 高清配音模型；同日已从 Volcengine 预置与 `/api/models?category=voice` 返回中移除 `豆包语音 TTS 2.0 / doubao-seed-tts-2.0`，创作页不再展示该配音模型按钮；同日创作页底部模型下拉按钮也已按后端真实 `voice` 分类拉取配音模型，并优先展示模型名称而非内部 `model_id`，避免用户已配置配音模型时按钮为空白或显示内部标识；同日也已把创作页配音输入区左侧 `44x60` 叠卡按钮接到 `DubbingVoiceModal`，现可直接选择 miioo 音色、自定义音色或收藏音频，并在提交配音生成时透传 `voice_id / reference_audio_url`；同日晚些时候配音结果卡片列表也已在展示层按音频 `id` 从大到小排序，避免恢复历史作品后顺序与用户预期不一致；同日最新已继续收口创作视频下载命名：`CreationPage.jsx` 现会在视频卡片下载、批量下载与详情弹窗下载时优先使用作品 `createdAt`，统一生成为 `YYYYMMDD-HHmmss.mp4`，没有生成时间时再回退当前时间，便于本地落盘后按时间识别视频文件；`2026-06-17` 已继续修复 `src/api/creation.js` 分页归一化里的构建级语法错误，当前 `pageSize` 回退链已拆为中间变量，不再因 `??` 和 `||` 混用触发 `vite:oxc` 解析失败，同时保留既有分页兼容顺序不变；`2026-06-22` 又继续把创作页视频里的提示词 `@` 数字资产绑定能力正式复用到图片与配音：当前 `CreationPage.jsx`、`src/utils/creationPromptAssetBinding.js` 与 `src/api/creation.js` 已统一收口 `prompt_raw / prompt_resolved / mentions / attachments` 生成链，图片继续沿用原绑定口径，配音则改为“`@资产` 只做参考绑定、不进入 TTS 正文”，并把被 `@` 的音频资产优先转成 `reference_audio_url` 下送后端 |
| 资产库 | 已打通，待真实联调 | 项目资产/创作资产、详情、回收站、批量下载已接通；`2026-06-11` 已继续把资产页下载入口统一收口到真实后端 blob 下载：项目整包下载、创作资产单项下载、创作资产批量下载，以及图片详情弹窗下载均不再依赖空回调或直链猜测；`2026-06-18` 又继续修复创作视频原画下载回退顺序，当前后端 `GET /api/assets/{asset_id}/download` 与创作视频单个/批量下载已统一优先尝试 `metadata_json.download_url`，再回退 `origin_url` 与 `file_url`，从而避免资产库“创作资产-视频”详情弹窗下载误下到预览/托管文件 |
| 剪辑成片 | 已打通，待联调 | 四区结构、时间线编辑、保存导出、结果回显已具备；`2026-06-11` 已把素材池从“视觉/音频两栏”调整为“图片/视频/音频三栏”，继续复用原卡片样式与加入时间线/音轨交互 |
| 选择类弹窗 | 已统一 | AssetPicker、VoiceLibrary、ReferenceAudioLibrary 视觉与交互已统一收口 |

## 3. 当前重点

- 继续验证真实模型与真实素材上传链路
- 继续验证任务状态回流、失败提示与下载结果
- 继续验证普通用户共享管理员 AI Ping 高清配音模型时的模型可见性、官方音色获取与实际生成成功率
- 控制页面样式调整范围，只做最小必要交互承接
