# 前端进度：核心工作流

## 1. 范围

本文件只收口前端核心工作流相关进度：

- 首页与登录
- 项目列表与新建项目
- 全局设定
- 剧本
- 主体
- 分镜

## 2. 当前状态

| 模块 | 当前状态 | 备注 |
|---|---|---|
| 首页与登录 | 已打通，待真实联调 | 登录、消息中心、定点触发登录与 API 配置入口已接通；`2026-06-11` 已按 `add-frontend-lazy-cache` 在 `App.jsx / Home.jsx` 为首页入口和登录/API/通知/资料/新建项目/水印等非首屏弹窗补齐懒加载边界与加载占位，同时为当前用户、通知等读请求接入统一 TTL 缓存、并发去重与 `force` 强刷入口 |
| 项目列表与新建项目 | 已打通，待真实联调 | 列表、新建、模板浏览、风格加载、封面上传已接通；`2026-06-11` 已继续把 `ProjectList / GlobalSettings / SubjectPage / StoryboardPage / AssetsPage / CreationPage` 接入工作台级懒加载，保持现有导航和页面骨架不变；`src/api/project.js` 同日也已为项目列表/详情/概览接入请求层 TTL 缓存、写后按 tag 失效与显式强刷能力 |
| 全局设定 | 已打通，待真实联调 | overview、基础信息自动保存、步骤解锁已接通 |
| 剧本 | 已打通 | 单剧本工作区、流式输出、分集结构、整稿拆分与提取主体已接通 |
| 主体 | 已打通 | 角色/场景/道具卡片、参考图、音色、批量生成已接通 |
| 分镜 | 已打通，专项收口中 | 分镜 CRUD、图片/视频生成与专项双层字段承接持续收口 |

## 3. 当前重点

- 核心主链路的真实环境联调
- 分镜专项 `SBX-03 ~ SBX-05`
- 继续保持最小样式改动，避免为联调破坏既有产品表达
